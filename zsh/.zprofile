# Variables de entorno
if [[ -z "$EDITOR" ]]; then
	export EDITOR="nvim"
if

if [[ -z "$LANG" ]]; then
	export LANG="es_ES.UTF-8"
if

if [[ -z "$VISUAL" ]]; then
	export VISUAL="nvim"
if

if [[ -z "$BROWSER" ]]; then
	export BROWSER="brave"
if
